function dictionary (input) {
    let wordDef = input[0];
    let givenWords = input[1];
    let command = input[2];
    let dictionary = {};
}

dictionary(["programmer: an animal, which turns coffee into code | developer: a magician",
"fish | domino",
"Hand Over"])